// backend/src/controllers/walletController.js
export const getWallet = (req, res) => {
  res.json({ message: "Get wallet endpoint working!" });
};

export const updateWallet = (req, res) => {
  res.json({ message: "Update wallet endpoint working!" });
};
